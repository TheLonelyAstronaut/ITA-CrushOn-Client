import styled from 'styled-components/native';

export const PassionsContainer = styled.View`
    flex-direction: row;
    flex-wrap: wrap;
    padding-horizontal: ${(props) => props.theme.spacer * 2.5}px;
`;
