export enum TextType {
    name,
    geo,
    header,
    regular,
    cardName,
    cardGeo,
    label,
    button,
}

export enum SeparatorVerticalType {
    large,
    medium,
    small,
    extrasmall,
}
